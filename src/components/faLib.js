import {library} from "@fortawesome/fontawesome-svg-core";

import {faCloud, faDatabase, faLaptopCode, faPager, } from "@fortawesome/free-solid-svg-icons";

library.add(
    faCloud,
    faDatabase,
    faLaptopCode,
    faPager,
);